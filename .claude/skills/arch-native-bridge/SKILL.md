---
description: Load the Rust C ABI native operations bridge reference for platform-specific FFI code
---

Load the Rust C ABI native operations bridge reference.

$READ docs/contributing/native-operations.md

Covers: platform matrix (Panama/PanamaPort/JS/Native/@extern), Rust workspace structure,
C ABI naming, adding new native operations. Load when modifying sge.platform code.
